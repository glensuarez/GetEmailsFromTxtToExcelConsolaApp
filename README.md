# Get Emails from Txt to  Excel (Python)

La app de consola lee un archivo de texto(txt), extrae correos electrónicos utilizando una expresión regular y los guarda en un archivo de Excel en una columna única.

